# 归属与出处

本仓库里能看到的每一样东西，要么是勘境自己写的，要么是别人的。这一页把两者分开，
并且说清各自适用什么许可。混淆这两类，是材料类仓库最常见的失德方式——
用别人的成果给自己加分量，同时不给对方任何署名。

## 三类材料

| 类别 | 在哪里 | 谁的 | 许可 |
| --- | --- | --- | --- |
| 本仓库自己写的目录、正文、脚本、生成物 | `curriculum/` `data/` `scripts/` `docs/` `site/` `tests/` | KineWorld 勘境 | MIT（见 [`LICENSE`](LICENSE)） |
| **被索引**的第三方开源项目 | `data/oss-resources.yaml` → [`docs/RESOURCES.md`](docs/RESOURCES.md) | 各项目原作者 | **各自原有许可，不由本仓库的 MIT 覆盖** |
| 被登记的外部平台与站点 | `data/links.yaml` → [`docs/LINKS.md`](docs/LINKS.md) | 各平台 | 各平台自己的服务条款 |

一句话：**本仓库没有 fork 任何项目。** 索引只是链接和说明，代码从未被复制进来，
因此本仓库的许可证不影响它们中的任何一个，也没有任何一段它们的历史或版权
出现在这里。

## 被索引的第三方项目

**唯一真源**：[`docs/RESOURCES.md`](docs/RESOURCES.md)（由 `scripts/build.py`
从 `data/oss-resources.yaml` 与 `data/oss-verified.json` 生成）。

那两份数据合起来记录了每个项目的：仓库全名、语言、类型、对应的知识点、
它解决什么问题、怎么用、以及**它的限制**。许可证与归档状态不写在这两份文件里，
而是每次由 `scripts/fetch_oss.py` 从 GitHub API 现拉，落进 `data/oss-verified.json`。
理由：手抄的许可证和星数会漂移，而漂移的归属信息比没有更糟——它看起来是核实过的。

为什么真源放在生成物里而不是这一页：这里列一遍、那里列一遍，两份清单一定会
分叉，然后没人知道该信哪份。这一页只讲规则，不讲清单。

### 被索引意味着什么、不意味着什么

| 它意味着 | 它不意味着 |
| --- | --- |
| 我们读过这个项目，并认为它和某个知识点的出口标准对得上 | 它是我们写的 |
| 读者可以按索引去用它 | 它已经被我们验证、复现或评测过 |
| 它的许可允许我们引用和链接它 | 它的许可允许你任意使用它捆绑的权重、数据集或示例素材 |
| 它是一个可用的起点 | 我们推荐它优于其它同类项目 |
| `caution` 字段写了它的限制 | 它的限制只有这些 |

**没有任何一个被索引的项目与本仓库存在合作关系，也没有任何一个为本材料背书。**
这一句是硬性的：即使某个项目作者表达了支持，在拿到可公开引用的凭据之前，
本页也不会这么写。

### 已知的两个特殊条目

- `Khan/khan-exercises` 已归档（GitHub 上的只读状态）。它被保留在索引里作为**反例**，
  说明"知名机构的开源项目也会停止维护"，`caution` 字段写了这一点。保留它比删掉有用。
- 部分被索引项目**没有声明许可证**。这类项目在 `docs/RESOURCES.md` 里会被标成
  `NO-LICENSE（未声明）`。未声明许可证**不等于**可以自由使用——默认是保留全部权利。
  需要商用或再分发时，请回到项目仓库确认，不要以本页为依据。

## 外部平台与站点

`data/links.yaml` 登记的是平台级地址（国家中小学智慧教育平台、Khan Academy、
PhET、MIT OpenCourseWare、B 站等），不是具体视频。每个知识点另有一个检索关键词，
拼成对应平台的检索页。

这不只是技术选择，也是归属选择：**指向检索页，读者看到的是平台自己排的序**；
指向具体视频，等于我们在替平台做推荐，而推荐需要核验，核验会过期。
具体视频一旦下架，我们留下的就是一个死链和一句过时的推荐。

链接的探测状态记录在 [`data/links-status.json`](data/links-status.json)，判据：
只有服务器明确返回 404/410 才算失效；本机连不上或被挡（403/429/5xx）只算
"这次没验成"。把网络抖动写成失效，报告就在说谎。

## 组织级文件

本仓库**不复制**组织的社区文件，而是继承并链接：

- 贡献规则：组织级 [`CONTRIBUTING.md`](https://github.com/kineworld/.github/blob/main/CONTRIBUTING.md)
  适用于本仓库；本仓库另有一份补充说明，只写本仓库特有的部分。
- 测试采集守卫（`tests/check_collection.py`、`tests/__init__.py`）是组织级
  [`kineworld/.github/guards/`](https://github.com/kineworld/.github/tree/main/guards)
  的**逐字节副本**。CI 会把它与原件比对，漂移即失败。这里保留副本而不是引用，
  是因为它必须能在本仓库的 CI 里离线运行；之所以要逐字节相同，是因为一份
  "大致相同"的守卫会在原件修好之后继续用旧的诊断糊弄所有人。

## 发现归属问题怎么办

如果本仓库漏了署名、用了不该用的材料、或者把别人的成果说成自己的，请开 issue
指出：哪一个文件、哪一句、原始出处是什么。**归属问题按缺陷处理，不按观点分歧处理。**
确认后会更正或移除。

## 本仓库不做的声明

- 本仓库不对任何第三方项目的性能、排名或能力做声明。
- 本仓库的存在不表示与任何第三方存在合作、赞助或背书关系。
- 本仓库不对任何第三方知识产权提出主张。
- 本仓库的学习路径不声称覆盖了任何学科的完整前沿——它是基础阶段的地图。
