# 为本仓库贡献

组织级 [CONTRIBUTING.md](https://github.com/kineworld/.github/blob/main/CONTRIBUTING.md)
适用于本仓库，**请先读它**。这一页只写本仓库特有的部分，不重复它——两份规则
写两遍，就一定会分叉，然后没人知道该信哪份。

短版：**先给证据，再给结论**，并且**你加的每一条检查都要能说出"什么缺陷会让它变红"**。
回答不出这个问题的检查是装饰品，比没有更糟：它给出"已经验过了"的错觉。

## 改内容：改源文件，不要改生成物

```
curriculum/*.yaml        知识点正文 —— 人工维护
data/links.yaml          整站链接登记 —— 人工维护
data/oss-resources.yaml  开源项目索引 —— 人工维护
        ↓  脚本
docs/*.md  site/index.html  生成物 —— 不要直接手改
```

`docs/` 和 `site/` 是生成物。直接改它们，下一次构建就把你的改动覆盖掉，
而 CI 的 `git diff --exit-code` 会把它标成"源文件改了但没重新生成"。
改内容请改 `curriculum/` 与 `data/`，然后跑：

```bash
python scripts/validate.py
python scripts/build.py
```

## 新增一个知识点

在对应学段的 `curriculum/<stage>.yaml` 里加一项，五个字段一个都不能少：

```yaml
- id: mid-math-02            # 必须用本学段前缀：pri- mid- hi- uni- lang-
  title: 整式与因式分解
  grade: 八年级
  core: true                 # 主干知识点
  strand: structure          # 必须是八条主线之一
  from: mid-math-01          # 主干必填：从哪个知识点推出来（或写 axiom: true）
  kid: 把一串式子里的公共部分提到前面去，就像把礼物盒里的东西拿出来排好。
  principle: >
    因式分解是把一个整体拆成更小的乘积。乘法与分解互为逆过程，
    所以分解的结果可以用乘回去检验——这是自查的通用套路。
  model: 能把自己的结果倒推回输入，才算真的算对。可逆性是最便宜的自检手段。
  exit: 能分解常见的二次三项式，并用乘回去验证每一步。
  video:
    q: 因式分解 入门        # 检索关键词，中文走 B 站与国家平台，英文走 YouTube/OCW
    c: [smartedu]           # 可选：data/links.yaml 里已登记的整站 key
```

五个写作要求，按重要性排：

1. **`kid` 用一年级能听懂的话。** 出现该学段之后的术语就是不合格的。
   宁可说得笨，不要说得虚。这一条目前靠人工审查，没有脚本检查——所以
   它写在文档里，不假装已经被自动检查过。
2. **`principle` 要说清从哪条更基本的事实推出来。** "因为书上这么写"不是第一性原理。
   主干知识点用 `from` 或 `axiom` 把这件事写进数据：

   - `from: <知识点 id>` —— 这一条从它推出来。链条要能一路走到某个起点。
   - `axiom: true` —— 这一条就是起点，当作给定的事实，不再往下追问。

   两者互斥。学段在 `_index.yaml` 里标了 `refined: true` 之后，
   该学段每条主干缺了这两个字段、`from` 指向不存在的 id、指向自己、
   或者链条成环，`check_derivation_chain` 都会让校验失败。
   **它管的是链条结构，管不了推导讲得对不对**——那件事只能靠人读，
   所以这里不假装它被自动检查了。
3. **`model` 要连上八条主线之一，并说清它在理解世界如何运作中担任什么角色。**
   这一条是整套材料的骨，不是装饰。
4. **`exit` 必须是一个能当场做一遍的动作。** 能算一个数、能画一张图、能写一行
   代码、能说清一个反例。"理解""掌握"不算——没人能判断它做完没有。
   这条已经脚本化（`check_exit_is_an_action`），写成"能理解……"会直接让
   `python scripts/validate.py` 退出码非零。它只拦模糊动词**处于支配位置**的写法，
   拦不住"看起来是动作、其实做不到"的那种，后者仍要靠人读。
5. **精写完一个学段，才把 `_index.yaml` 里它的 `refined` 改成 `true`。**
   改早了会立刻红，这正是它存在的意义：那句声明必须兑现。

## 措辞红线

`scripts/validate.py` 里有一组正则，命中就让构建失败。它们拦的是具体的承诺句式，
不是词语本身：

- 保证性承诺（"保证学会""一定能考上"）——**"不能保证"是合法且必要的表述**，
  否定形式会放行。
- 排名与最强声明（"全球第一""全网最强"）。
- 最高级断言（"最权威的认知"）。
- 轻率承诺（"读完就能……"）。

**"前 1%" 只能作为目标出现，且同一行里必须有"目标""不是承诺"之类的限定语。**
这条是仓库主人的原始目标措辞，也是最容易被后来的贡献者改成承诺句的地方，
所以单独一条检查盯着它（扫仓库根部全部 Markdown）。

要加新的红线：先想清楚它拦的是**句式**还是**词语**。拦词语的红线会把你想要的
诚实表述一起干掉。

## 加一条检查

按组织的规则，加检查之前先回答：**什么缺陷会让它变红？**

答不出来就别加。答得出来，就同时做三件事：

1. 把检查函数写进 `scripts/validate.py`，并加进模块顶部的 `ALL_CHECKS` 清单。
2. 在 `tests/test_every_check_can_fail.py` 里加一条用例，故意造出那个缺陷，
   断言检查确实报错；并把检查名登记进该文件的 `COVERED` 表。
3. 跑 `python -m unittest discover -s tests -t .`。

漏了第 2 步不会有人骂你——`CoverageOfChecksTest` 会直接红，并告诉你缺哪一条。

那个文件里还有两条自己给自己上的箍，改测试时别绕过它们：

- **基线必须干净。** 最小目录树本身有问题，所有"能变红"的用例都是假阳性。
- **`set_root` 必须把全部路径都搬过去。** 漏掉一条，检查会静默地对着真实仓库跑，
  于是全绿，什么也没验。

## 改链接与开源索引

- `data/links.yaml` 只放**平台级**地址，不放具体视频（理由见
  [`ATTRIBUTION.md`](ATTRIBUTION.md)）。每条都要真的访问过：
  `python scripts/check_links.py`。
- `data/oss-resources.yaml` 里**不要写星数和许可证**。那些数字由
  `scripts/fetch_oss.py` 现拉。手抄的数字会漂移，而漂移的归属信息比没有更糟——
  它看起来是核实过的。
- 写仓库名请与 GitHub API 返回的 `full_name` 大小写完全一致。校验器会拿
  `data/oss-verified.json` 比对，不一致就报错（实测中真的发现过两个项目改了名）。
- 每个条目必须写 `caution`。**只写优点的推荐不算推荐。** 写清它不适合什么、
  哪里会坑人、什么情况下别用它。
- 收录标准三条：GitHub API 能查到；能对上某个知识点的出口标准；能说清限制。

## 改测试

- 测试函数名必须以 `test_` 开头，且**在模块顶层定义或写在 `TestCase` 里**。
  写在 `if` 语句块里的不会被采集，`tests/check_collection.py` 会因为
  "源文件定义了 N 个、运行器只看到 M 个"而红。
- 别改 `tests/check_collection.py` 与 `tests/__init__.py`。它们是组织级守卫的
  逐字节副本，CI 会与原件比对。要改就改组织原件，然后重新复制。
- 测试里要换根目录，用 `_common.set_root(tmp)`，并务必在 `addCleanup` 里恢复。
  忘了恢复会让后面的用例对着上一棵树的缓存跑，表现是"偶发失败"。

## 提交前跑一遍

```bash
pip install -r requirements.txt
python scripts/validate.py -v
python -m unittest discover -s tests -t .
python tests/check_collection.py
python scripts/build.py && git diff --exit-code -- docs site
```

把结果贴进 PR 说明。CI 跑的是同一套命令。

## 不要提交

密钥、凭据、模型权重、数据集、第三方受限素材、以及任何"来源不明的视频"。
需要权重或受限数据时，在 PR 说明里写清获得方式，不要把文件放进来。

## 评审与响应

目前**一位维护者**，兼职做这件事，回复以天计不以小时计。我们不设服务等级，
并且会在讨论结论之前先要原始依据。这不是刁难——先把依据拿到，后面的讨论才有意义。
