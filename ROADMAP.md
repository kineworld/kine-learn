# 路线图与出口标准

这个文件回答两个问题：**现在做到哪一步了**，以及**每一步怎么算做完**。

"怎么算做完"一律写成可检查的动作：能跑出一条命令、能交出一个文件、能回答一个
具体问题。写成"深入理解""熟练掌握"的，等于没写——没人能判断它做完没有，
于是永远做不完，也永远不算没做完。

**关于"全球前 1% 的认知水平"：那是方向，不是承诺，也不在下面的出口标准里。**
它无法由一份材料单方面兑现，取决于练习量和纠错质量。能被检查的只有每一阶段
自己的出口标准——那些才写在这里。

---

## 阶段一：骨架落地（已完成）

**做了什么**

- 五个学段的目录结构与知识点正文：小学、初中、高中、大学基础、语言（英语与中文）。
- 每个知识点五个必备字段：一年级版、第一性原理、与世界模型的连接、出口标准、视频入口。
- 八条世界认知主线，作为横穿所有学科的骨。
- 外部资源登记：整站链接 + 检索模板 + 开源项目索引。
- 四条脚本：内容校验、链接探测、元数据拉取、离线构建。
- 生成物：`docs/*.md` 与单文件站点 `site/index.html`。

**出口标准（可检查）**

```bash
python scripts/validate.py            # 退出码 0
python -m unittest discover -s tests -t .   # 全绿
python tests/check_collection.py      # "every test ... is visible to the runner"
python scripts/build.py && git diff --exit-code -- docs site   # 无 diff
```

**这一步刻意没做**：没有把每个知识点都写成完整课文。骨架 + 核心精写优先——
先把"该学什么、为什么是这些"钉死，再往里填字。填字的速度远快于改结构。

## 阶段二：主干精写（进行中）

**目标**：每个学段的主干知识点（`core: true`）都有能独立读懂的第一性原理讲解，
而不是一个提纲句。

**出口标准**

1. 每个主干知识点的 `principle` 长度足以独立成段，且能指出它从哪条更基本的事实推出。
2. 每个主干知识点的 `exit` 是一条可以当场做一遍的动作（算一个数、画一张图、
   写一行代码、说清一个反例），不是"理解"。
3. `kid` 字段里不出现该学段之后的术语。检查方式：人工抽查，并把抽查结论写进
   PR 说明——这一条暂时无法脚本化，所以它写在路线图里而不是校验器里，
   不假装已经被自动检查过。
4. 每新增一个知识点，`docs/` 与 `site/` 同步重新生成。

当前覆盖数字见 [`docs/COVERAGE.md`](docs/COVERAGE.md)。那个文件由
`scripts/build.py` 现算，不手打——手打的数字和源文件漂移之后没人会发现。

## 阶段三：出口标准变成练习

**目标**：把 `exit` 从一句话变成可以真的做一遍、并且能判断对错的东西。

**出口标准**

- 每个主干知识点至少配一个可自查的练习：给出题目、给出判据、给出常见错法。
- 练习与知识点在数据里双向可查：从知识点能找到练习，从练习能回到知识点。
- 校验器新增一条检查：主干知识点没有配练习时报错。**并且**要为这条检查写一条
  能弄红它的用例，否则它不算数。

阶段二之前不做这一步。没有讲清原理就先出题，只会变成刷题。

## 阶段四：外部资源从"检索"升级为"核验"

**目标**：把每个知识点的视频入口，从检索关键词升级为**已实际看过并确认对得上**
的具体资源。

**出口标准**

- 每个主干知识点至少有一条已核验的具体资源（记录标题、平台、时长、覆盖范围）。
- 每条核验记录包含**它不适合什么**：哪一段讲得含糊、哪里和本材料口径不一致。
  只记录优点的推荐不算核验。
- 失效资源登记为负向结果，保留记录，而不是悄悄删掉。

为什么排在后面：具体视频会下架改名。核验工作只有在材料结构稳定之后做才不会白做。

## 阶段五：与世界模型工作流接通

**目标**：让学完的人能直接投入勘境正在进行的工作，而不是学完不知道能干什么。

**出口标准**

- 每个知识点的 `model` 字段能指回一个具体的工作场景：读一篇论文时用得上、
  评测一个模型时用得上、或者调试一个训练流程时用得上。
- 产出一份"最小可参与清单"：从本材料出发，到能对一个真实任务做出可被检查的
  贡献，中间需要经过哪些步骤。

---

## 不打算做的事

- **不做排名。** 不比较哪套教材更好，不给资源打分排名。列出限制比给出名次有用。
- **不做承诺。** 不写"学完就能如何"。措辞红线由 `scripts/validate.py` 强制。
- **不做权重与数据集的仓库。** 本仓库只做学习路径与索引，不托管模型权重、
  训练数据或第三方受限材料。
- **不把检查做成装饰。** 任何新加的自检，如果回答不出"什么缺陷会让它变红"，
  就不合并。

## 怎么改这份路线图

提 PR，说明改了哪个阶段、出口标准为什么需要调整、以及新的标准如何检查。
如果新标准无法检查，请同时说明为什么它仍然值得写进来——以及它什么时候会被
移出去。
